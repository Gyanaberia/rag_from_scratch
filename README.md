# rag_from_scratch
A phased, barebones build of a Retrieval-Augmented Generation (RAG) system — from a single API call to a full chat interface backed by a knowledge base. Built entirely with free tools. 
